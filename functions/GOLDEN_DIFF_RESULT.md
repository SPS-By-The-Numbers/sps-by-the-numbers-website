# Golden diff result — PASS ✅

Date: 2026-07-25
Golden: data-tools reference/regress/bigsheet_golden_seattle.csv
  (generated by frozen marts.bigsheet; 1185 data rows, 1240 columns + pandas index col 0)

Command (from functions/, tsx + ADC):
  npx tsx scripts/golden_diff.ts diff <golden.csv> 17001

Result:
  Columns: golden 1240, actual 1240 — set + order MATCH
  Rows:    golden 1185, actual 1185
  Key misses: 0
  Value mismatches: 0
  === GOLDEN DIFF CLEAN ✅ ===

The SQL pipeline (functions/src/bigsheet) reproduces the Python bigsheet.py
output for Seattle value-for-value, keyed by (class_of, school_code).

Notable fidelity points proven by the pass:
  - pivot column ordering (assessment/MAP/SQSS) = full-tuple first-appearance
  - exact order-statistic percentiles (numpy inverted_cdf), not APPROX_QUANTILES
  - null-safe (class_of, school_code) joins for district-total rows
  - dummies computed in the vitals branch (NULL, not 0, on outer-join-only rows)
  - COALESCE spend groups; COALESCE(SUM) churn (pandas min_count=0)
  - normalized fields windowed over the pre-sqss frame (NULL for sqss-only rows)
  - IEEE_DIVIDE per-pupil/normalized (matches numpy inf/nan on all_students=0)
  - SQSS "Non Migrant"/"Non-Migrant" (+64 more) sanitize collisions
    disambiguated deterministically; both generator and harness agree.
