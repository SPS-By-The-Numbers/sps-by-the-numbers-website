This directory defines filters that represent the UI structure + grouping of items
for filters. It also has translation functions into the underlying representation
which does NOT include grouping.

In comparison, the `_settings` directory has serialization/deserialization
routines base on clusters of related values.
